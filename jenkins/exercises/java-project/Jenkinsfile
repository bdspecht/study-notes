pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'ant -f jenkins/exercises/java-project/build.xml -v'
      }
    }
  }

  post {
    always {
      archive 'jenkins/exercises/java-project/dist/*.jar'
    }
  }
}
